/*
Funciones: LIBRERIA STRING.H
  1. strlen() ----> Devuelve longitud de cadena
      strlen(cadena)
  2. strcpy() ----> Perimte asignar un nuevo valor a una cadena
      strcpy(cadena,"Nuevi valor")
  3. strcat()  ----> Permite concatenar valores a una cadena
      strcat(cadena,otra_cadena)
  4. strcmp() ----> Perimite comparar 2 cadenas (está incluida en <stdio.h>)
      strcmp(cadena,otra_cadena)
          Si son iguales -> si da 0
          Si cadena esta antes de otra_cadena >0
          Si cadena esta despes de otra_cadena <0
  5. strstr() ----> Busca una cadena dentro de otra cadena
*/

#include<stdio.h>

void main(){


  return;
}